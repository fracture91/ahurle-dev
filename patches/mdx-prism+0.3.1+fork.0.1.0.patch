diff --git a/node_modules/mdx-prism/add-markers.js b/node_modules/mdx-prism/add-markers.js
index ee63c6d..397d642 100644
--- a/node_modules/mdx-prism/add-markers.js
+++ b/node_modules/mdx-prism/add-markers.js
@@ -14,10 +14,12 @@ const lineNumberify = function lineNumberify(ast, lineNum = 1) {
 
         const lines = node.value.split('\n');
         for (let i = 0; i < lines.length; i++) {
+          if (i !== 0) ++lineNumber
+          if (i === lines.length - 1 && lines[i].length === 0) continue
           result.nodes.push({
             type: 'text',
             value: i === lines.length - 1 ? lines[i] : `${lines[i]}\n`,
-            lineNumber: i === 0 ? lineNumber : ++lineNumber
+            lineNumber: lineNumber
           });
         }
 
